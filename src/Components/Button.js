import React from "react";

function Button({index}){

    // return (
    //     <div key={index+1}>
    //         <button onClick={() => setInput(input)} className="buttons"> {index+1} </button>
    //     </div>
    // )



}

export default Button;